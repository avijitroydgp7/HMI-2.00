"""
Runtime Simulator package

This package contains the PC runtime engine that loads a saved
HMI project (*.hmi JSON) and runs it in an interactive window.

The simulator is intentionally separated from the designer so
both can be run as independent executables.
"""

